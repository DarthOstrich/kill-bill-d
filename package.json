{
  "name": "kill-bill-d",
  "version": "1.0.0",
  "description": "Interactive CLI to find and delete build output directories",
  "type": "module",
  "bin": {
    "kill-bill-d": "./bin/kill-bill-d.js"
  },
  "files": ["bin", "src"],
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "fast-glob": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}
